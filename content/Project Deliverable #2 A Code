begin
    using CSV
    using DataFrames
    using PlutoUI
    using Plots
    using RDatasets
    using Measures
    using StatsPlots
    using Statistics
    using Distributions
    using LinearAlgebra
    using Random
end
df = CSV.read("C:/Users/STP/Desktop/ConcreteProjectData.csv", DataFrame)
describe(df)
Cement = df[:,1] 
BlastFurnaceSlag = df[:,2]
FlyAsh = df[:,3]
Water = df[:,4]
Superplasticizer = df[:,5]
CoarseAggregate = df[:,6]
FineAggregate = df[:,7]
Age = df[:,8]
CementCompressiveStrength = df[:,9]
Sample = df[:,10]
#plot(Age,CementCompressiveStrength)
#plot(Cement,CementCompressiveStrength)
describe(df)
print(df)
#FA = select(df, [Cement (component 1)(kg in a m^3 mixture):, :Sample])
df
abs
